{
    "check": {
	"apply_on": "mysql",
        "script": "$nagiosplugins$/check_mysql_health --user $mysql.user$ --password $mysql.password$ --mode threads-created",
        "interval": "60s"
    }
}
