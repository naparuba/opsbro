detector:
    # Which groups to set
    add_groups:
        - 'ovh'
    # will be set only if
    apply_if: '{{collector.geoloc.org}}) != "" and "ovh" in string_lower({{collector.geoloc.org}})'
    interval: 3600s