FROM centos:7
MAINTAINER Jean Gabes <naparuba@gmail.com>


# We want to have fr_FR.UTF8 as locales, but for this we must hack a bit the docker image
RUN        sed 's/^override_install_langs/#override_install_langs/' -i /etc/yum.conf
RUN        yum -y -q reinstall glibc-common
#ENV        LANG=fr_FR.UTF8
ENV        LANG=fr_FR33.UTF8

# Check that french is active
#RUN          ls /notexists_ 2>&1 | grep 'Aucun fichier ou dossier de ce type'


ADD        . /root/opsbro-oss

WORKDIR    /root/opsbro-oss
ENTRYPOINT        test/test_one_linux_installation.sh  && test/assert_group.sh "centos"


