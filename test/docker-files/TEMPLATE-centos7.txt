FROM       centos:7
MAINTAINER Jean Gabes <naparuba@gmail.com>


ADD        test/docker-helper/  /

# THIS DOCKER FILE is USED TO PUSH TO napruba/centos-7 so tests will be faster, and won't need to recompute all of this every time

RUN        /yum_install  libgomp
RUN        /yum_install  epel-release
RUN        /yum_install  python-jinja2
RUN        /yum_install  python-crypto
RUN        /yum_install  python-setuptools
RUN        /yum_install  nss  # for epel https
RUN        /yum_install  rpm-python
RUN        /yum_install  python2-leveldb   # currently disabled before we change tests to give leveldb by default every where
RUN        /yum_install  python2-simplejson
RUN        /yum_install  python-setproctitle
RUN        /yum_install  python2-psutil
RUN        /yum_install  python2-yaml
RUN        /yum_install  bash-completion

ENTRYPOINT     /bin/bash
