FROM       naparuba/debian-12
LABEL maintainer="Jean Gabes <naparuba@gmail.com>"



ADD        . /root/opsbro-oss
WORKDIR       /root/opsbro-oss

RUN       $PYTHON_EXE setup.py install


RUN       rm -fr /etc/opsbro/zones/*
ADD       test/test-files/test-gossip-zones-multi/zones/*   /etc/opsbro/zones/


# NOTE: ARG must be AFTER the FROM, and with ENV to access it
ARG        NODE_NB=
ENV        NODE_NB=$NODE_NB


# The node1 will try to connect to node2 and auto join it
ENTRYPOINT    test/test_duo4_gossip_zones_multi.sh "$NODE_NB"
