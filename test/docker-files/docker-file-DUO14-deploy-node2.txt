FROM       naparuba/debian-12
LABEL maintainer="Jean Gabes <naparuba@gmail.com>"


RUN        /apt_get_install   openssh-server


# NOTE: node2 do NOT have opsbro :)
# but only ssh server launched
# + need the test files
ADD        . /root/opsbro-oss
WORKDIR       /root/opsbro-oss



# Copy the ssh client key into the user
ADD        test/test-files/test-deploy/node2/.ssh  /root/.ssh
RUN       chmod -R 600 /root/.ssh


# The node2: just is a ssh server
ENTRYPOINT    test/test_duo14_deploy_node2.sh